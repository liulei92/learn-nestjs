import { HttpException2Filter } from './http-exception2.filter';

describe('HttpException2Filter', () => {
  it('should be defined', () => {
    expect(new HttpException2Filter()).toBeDefined();
  });
});
